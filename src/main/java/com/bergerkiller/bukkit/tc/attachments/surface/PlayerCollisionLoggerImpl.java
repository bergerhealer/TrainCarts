package com.bergerkiller.bukkit.tc.attachments.surface;

/**
 * Thin concrete logger that uses the default implementations in
 * {@link PlayerCollisionLoggerBaseImpl}. Kept as a separate class so callers
 * can instantiate a ready-to-use logger.
 */
public class PlayerCollisionLoggerImpl extends PlayerCollisionLoggerBaseImpl {
    // No additional behavior; uses defaults from PlayerCollisionLoggerBaseImpl
}
